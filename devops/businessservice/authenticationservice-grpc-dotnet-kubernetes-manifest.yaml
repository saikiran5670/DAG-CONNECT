apiVersion: apps/v1
kind: Deployment
metadata:
  name: authenticationservice
spec:
  selector:
    matchLabels:
      run: authenticationservice
  replicas: 2
  template:
    metadata:
      labels:
        run: authenticationservice
    spec:
      containers:
      - name: authenticationservice
        image: dafdev017756.azurecr.io/authenticationservicegrpc
        imagePullPolicy: Always
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: authenticationservice
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  selector:
    run: authenticationservice
